# Security

Do not report security issues in a public Jira work item or GitHub issue. Send a
private report to the repository owner through GitHub's security advisory
interface.

This repository contains prompts rather than credentials. Keep OAuth tokens,
API tokens, private URLs, employer data, and work-item content outside Git.
Connected tools act with the permissions of the authenticated user; follow
least privilege and require human review for high-impact external mutations.
